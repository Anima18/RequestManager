<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chirs.rxsimpledemo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.chirs.rxsimpledemo.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GetObjectDataActivity" android:label="getObject"/>
        <activity android:name=".GetCollectionDataActivity" android:label="getCollection"/>
        <activity android:name=".GetBitmapDataActivity" android:label="getBitmap"/>
        <activity android:name=".PostObjectDataActivity" android:label="PostObject"/>
        <activity android:name=".PostCollectionDataActivity" android:label="PostCollection"/>
    </application>

</manifest>